/* FedCatalog site text you can change without a rebuild.
   Edit the words between the quotes, save, upload this one file. Every page reads it.
   (The same text is also baked into the pages at build time, so search engines and
   visitors without JavaScript see the last built version until the next rebuild.) */
var FEDCATALOG_TEXT = {
  heroTitle: "Federal Software in One Place",                                  // home page headline
  heroSub: "FedRAMP, DoD, OneGov, GSA, SEWP and marketplaces. Connected."      // the one line under it
};
